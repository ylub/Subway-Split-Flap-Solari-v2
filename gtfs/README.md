# GTFS Feeds

Local GTFS feeds live here when running the app:

- `lirr/`
- `subway/`
- `subway_supplemented/`
- `manhattan_bus/`
- `metro_north/`

The feed contents are ignored by git. Keep source ZIPs in `zip/`, extract them here, and refresh them from MTA as needed.
